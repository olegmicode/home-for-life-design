import ReDocument from "./Document";


// Create Document Component
const ReportPDF = ({  data }) => {    
    return <ReDocument data={data} wrap>
        
    </ReDocument>
};

export default ReportPDF
